{
  // The same store, three ways, and what is actually different between them.
  //
  // Drawn so the reader can compare the ROWS rather than the columns: the
  // engine row is identical across all three, which is the claim. What
  // changes is the boundary drawn around it and how many databases sit
  // underneath.

  // The site's palette. dbo.css maps every one of these onto its own --dbo-*
  // and wins, because Lini's defaults sit in a cascade layer — so these
  // values are only what the figure looks like opened away from the site.
  --ink: #111a24;   --body: #2c3a47;      --muted: #5d6c7b;  --faint: #8695a3;
  --panel: #ffffff; --line: #e3e0da;      --accent: #a8641b; --bg: #fbfaf8;
  --seal: #14532d;  --seal-soft: #e7f0e9; --accent-soft: #f6efe4;

  // Lini's own default stroke is a THREE-digit hex pair, and a three-digit
  // hex is indistinguishable from an issue reference to the branding ratchet
  // — which strips the six-digit form and cannot strip this one. Stating the
  // colour at full length keeps the check honest rather than loosened; and
  // dbo.css maps --lini-stroke onto the site palette like the rest, so this
  // value is only what a figure looks like away from the site.
  --stroke: #444444;
  font-family: "Google Sans"; color: --body;

  // A BARE STRING IS ALWAYS CENTRED in its parent, whatever `align` says;
  // only a block-wrapped one can be placed. So every piece of text that must
  // sit left is wrapped, and its container aligns to start.
  |t::block|       { font-size: 13; color: --body; }
  |small::t|       { font-size: 12.5; color: --muted; }
  |cap::t|         { font-weight: semibold; color: --ink; }
  |eyebrow::t|     { font-size: 11; font-weight: bold; letter-spacing: 1.2;
                     text-transform: uppercase; color: --faint; }
  |col::block|     { layout: flow; direction: column; align: start; gap: 6; }
  |panel::box|     { radius: 9; fill: --panel; stroke: --line; stroke-width: 1.5;
                     padding: 14; align: start; }
  |sealed::panel|  { fill: --seal-soft; stroke: --seal; }
  |dashed::box|    { radius: 9; fill: none; stroke: --faint; stroke-width: 1.5;
                     stroke-style: dashed; padding: 14; align: start; }
  |line-item::box| { radius: 5; fill: --bg; stroke: --line; stroke-width: 1.25;
                     height: 26; padding: 0 12; align: start; justify: center; }
  |lt::t|          { font-size: 12; }
  |has::t|         { font-weight: semibold; color: --seal; }
  |miss::t|        { color: --faint; }
  |body::t|        { color: --muted; }


  layout: flow; direction: column; align: start; width: 760;
  padding: 4; gap: 0; clearance: 2;
}

|eyebrow| "One implementation, three deployments"
|block| { height: 10 } []

|block| { layout: grid; columns: repeat(3, 248); gap: 8 } [

  |panel| { width: 248; height: 196; padding: 14 } [ |col| { width: 220; gap: 7 } [
    |cap| "inside a host process"
    |small| "a test suite, a laptop,"
    |small| "an appliance on a shelf"
    |block| { height: 4 } []
    |line-item| { width: 220 } [ |lt| "the host's own JVM" ]
    |line-item| { width: 220 } [ |lt| "one Postgres beside it" ]
    |block| { height: 2 } []
    |small| "no container, no orchestrator" ] ]

  |panel| { width: 248; height: 196; padding: 14 } [ |col| { width: 220; gap: 7 } [
    |cap| "one node, many tenants"
    |small| "the ordinary deployment"
    |block| { height: 4 } []
    |line-item| { width: 220 } [ |lt| "one process, one port" ]
    |line-item| { width: 220 } [ |lt| "a database per tenant" ]
    |line-item| { width: 220 } [ |lt| "runners alongside, or not" ]
    |block| { height: 2 } []
    |small| "an orchestrator runs it and holds"
    |small| "the secrets; it routes nothing" ] ]

  |dashed| { width: 248; height: 196; padding: 14 } [ |col| { width: 220; gap: 7 } [
    |miss| "a fleet"
    |miss| "specified, not built"
    |block| { height: 4 } []
    |line-item| { width: 220; fill: none; stroke-style: dashed } [ |miss| "many nodes, one network" ]
    |line-item| { width: 220; fill: none; stroke-style: dashed } [ |miss| "a tenant has one writer" ]
    |line-item| { width: 220; fill: none; stroke-style: dashed } [ |miss| "entry near the requester" ]
    |block| { height: 2 } []
    |miss| "the caller's code does not change" ] ]
]

|block| { height: 14 } []
|sealed| { width: 760; height: 84; padding: 14 16 } [ |col| { width: 728; gap: 6 } [
  |has| "The row that does not change: the engine, the faces, the tenant's database, the trail."
  |small| "Which is why a consumer's test runs against the real store, and why what worked there"
  |small| "is expected to work in the others without being rewritten." ] ]
