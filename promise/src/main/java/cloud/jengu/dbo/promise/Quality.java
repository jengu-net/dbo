package cloud.jengu.dbo.promise;

/**
 * The technical view — performance, reliability, scale, operability (arc42 §10 / ISO 25010 quality attributes). Deliberately not "capability", which is a load-bearing word in the face contract.
 */
public interface Quality extends Classified {
}
