package cloud.jengu.dbo.promise;

/**
 * A concrete case, told from the participant's side — what somebody does and what the system holds up while they do it.
 */
public interface Story extends Classified {
}
