package cloud.jengu.dbo.promise;

/**
 * An externally imposed rule, mostly regulatory (arc42 §2) — the system does not choose it, only fulfils it.
 */
public interface Constraint extends Classified {
}
