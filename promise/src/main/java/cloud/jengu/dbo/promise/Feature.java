package cloud.jengu.dbo.promise;

/**
 * The system-functional view of the same ground the stories walk — what the system does, stated as function rather than as scenario.
 */
public interface Feature extends Classified {
}
