name: build

# THE dbo pipeline — one workflow, two modes:
#
#   main push / PR   suite → (push only) snapshot jars → images :main-<sha>
#   release tag v*   suite → RELEASE jars → images :X.Y.Z
#                    → Maven Central bundle (v-tags only)
#
# Releases pin everything to one number: the jars, the Bundle-Versions and
# the image tags. Nothing publishes and no image ships unless the full suite
# is green on exactly that commit.
#
# Jars go to the project's own PUBLIC repository. Central is a later
# destination for the same coordinates, blocked on artifact size rather than
# on anything about the build (RELEASING.md).
#
# Maven Central is deliberately RELEASE-ONLY and gated on a secret being
# present: Central is immutable, so a publish is not something a main-branch
# push should be able to do by accident.

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    # SELF-HOSTED because this repository is PRIVATE under a free-tier org:
    # GitHub-hosted minutes are a monthly budget that one busy day exhausts,
    # and a job that cannot start looks like a red build to everyone
    # downstream. Private also means fork PRs from strangers cannot exist,
    # which is the precondition — if this repository ever goes PUBLIC, these
    # jobs go back to hosted runners (or gain the pull_request guard the
    # images job has), because a public fork PR on a self-hosted runner is
    # arbitrary code execution on the machine that runs it.
    runs-on: self-hosted
    # the test reporter writes a check run and annotates failing lines
    permissions:
      contents: read
      checks: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v4
      - name: Check branding
        # Branding creeps back one commit at a time, and a public repository
        # that quietly regrows private issue numbers and internal hostnames is
        # worse than one that never cleaned them.
        run: .github/scripts/check-branding.sh
      - name: Resolve version
        id: version
        run: |
          if [[ "${{ github.ref_type }}" == "tag" && "${{ github.ref_name }}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "gradle_args=-Pdbo.version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
            echo "image_tag=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
            echo "is_release=true" >> "$GITHUB_OUTPUT"
          else
            echo "gradle_args=" >> "$GITHUB_OUTPUT"
            echo "image_tag=main-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
            echo "is_release=false" >> "$GITHUB_OUTPUT"
          fi
      # Daemons accumulate on a persistent runner and each carries heap the
      # VM cannot spare; the platform workflows learned this in #566.
      - name: Stop leftover Gradle daemons
        run: ./gradlew --stop || true
      - name: Build and test core
        # -P properties, NOT env vars: a long-lived daemon keeps the
        # environment it was born with, so env dials silently do nothing —
        # three OOM-killed runs learned that. The values are measured against
        # the runner VM's 6g-for-everything: the whole suite is green at this
        # heap and width locally, with room the terminology drop (#83) paid
        # for; 4g/width-4 was GitHub-hosted 16g sizing.
        run: >-
          ./gradlew build -PdboTestHeap=2048m -PdboTestParallelism=1
          ${{ steps.version.outputs.gradle_args }}
      # Gradle already writes JUnit XML; these two steps make it readable
      # WITHOUT scrolling the log (the CI-parity rule: verdicts, not logs).
      # Both run on failure too — that is the case worth reading.
      - name: Publish test results
        if: always()
        uses: mikepenz/action-junit-report@v5
        with:
          report_paths: "**/build/test-results/**/TEST-*.xml"
          detailed_summary: true
          # list what RAN, not only what broke — a green run that shows an
          # empty table cannot tell you whether it tested anything
          include_passed: true
          group_suite: true
          # durations make the critical path obvious — the suite runs its
          # classes concurrently, so the slowest class IS the wall clock
          include_time_in_summary: true
          check_name: dbo suite
      - name: Upload HTML test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: "**/build/reports/tests/**"
          retention-days: 7
    outputs:
      gradle_args: ${{ steps.version.outputs.gradle_args }}
      image_tag: ${{ steps.version.outputs.image_tag }}
      is_release: ${{ steps.version.outputs.is_release }}

  publish:
    needs: build
    if: github.event_name != 'pull_request'
    # self-hosted for the same budget reason as build; never on pull_request
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v4
      - name: Publish library modules
        # SNAPSHOT → maven-snapshots, fixed version → maven-releases; the
        # repository URL switches on the version suffix.
        env:
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
          SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
        run: >-
          ./gradlew publishAllPublicationsToJenguRepoRepository --no-daemon
          ${{ needs.build.outputs.gradle_args }}

  images:
    needs: build
    # LOAD-BEARING SECURITY, not a convenience: this job runs on a SELF-HOSTED
    # runner and pushes to a LAN registry. A public repository whose fork PRs
    # could reach a self-hosted runner is arbitrary code execution on the
    # machine that runs it. Removing this condition, or adding pull_request to
    # a job on this runner, is the whole vulnerability.
    if: github.event_name != 'pull_request'
    runs-on: self-hosted
    timeout-minutes: 30
    strategy:
      matrix:
        include:
          - image: dbo-operator
            dist: ":core:dbo-operator:installDist"
            context: core/dbo-operator
          - image: dbo-server
            dist: ":core:dbo-server:installDist"
            context: core/dbo-server
    env:
      # The LAN registry stays the fleet's own source — edge runtimes and the
      # k3s deployments resolve from it, and it must not depend on a public
      # registry being reachable. GHCR is the public primary; Docker Hub
      # mirrors it because that is where people look first. Both public
      # pushes are gated on their secret existing, so a fork builds images
      # without needing any of them.
      REGISTRY: 192.168.1.14:5050
      GHCR_IMAGE: ghcr.io/jengu-net/${{ matrix.image }}
      DOCKERHUB_IMAGE: docker.io/jengu/${{ matrix.image }}
      # A boolean, never the secret itself, and computed HERE because the
      # `secrets` context does not exist inside an `if:` — a workflow that
      # reaches for it there does not fail the job, it fails to parse, and
      # every push to the branch reports a run with no jobs at all.
      HAVE_DOCKERHUB: ${{ secrets.DOCKERHUB_TOKEN != '' }}
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Stop leftover Gradle daemons
        run: ./gradlew --stop || true
      - name: Build distribution
        run: ./gradlew ${{ matrix.dist }} --no-daemon ${{ needs.build.outputs.gradle_args }}
      - name: Authenticate to the LAN registry
        env:
          # These are the LAN registry's credentials. The names are historical
          # — they once belonged to a Maven repository that no longer exists —
          # and renaming them means adding two secrets before the next push.
          REG_USER: ${{ secrets.NEXUS_USERNAME }}
          REG_PASS: ${{ secrets.NEXUS_PASSWORD }}
        run: |
          set -euo pipefail
          mkdir -p ~/.docker
          AUTH=$(printf '%s:%s' "$REG_USER" "$REG_PASS" | base64 | tr -d '\n')
          printf '{"auths":{"%s":{"auth":"%s"}}}' "${{ env.REGISTRY }}" "$AUTH" > ~/.docker/config.json
      - name: Log in to GHCR
        continue-on-error: true
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Log in to Docker Hub
        id: dockerhub
        if: ${{ env.HAVE_DOCKERHUB == 'true' }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Assemble the tag list
        id: tags
        run: |
          set -euo pipefail
          {
            echo "public<<EOF"
            echo "${{ env.GHCR_IMAGE }}:${{ needs.build.outputs.image_tag }}"
            # On the PUBLIC registries `latest` follows releases only: somebody
            # who types no tag should not receive an arbitrary commit. The
            # fleet registry is different on purpose — that is how deployments
            # there pick up main.
            if [ "${{ needs.build.outputs.is_release }}" = "true" ]; then
              echo "${{ env.GHCR_IMAGE }}:latest"
            fi
            if [ "$HAVE_DOCKERHUB" = "true" ]; then
              echo "${{ env.DOCKERHUB_IMAGE }}:${{ needs.build.outputs.image_tag }}"
              if [ "${{ needs.build.outputs.is_release }}" = "true" ]; then
                echo "${{ env.DOCKERHUB_IMAGE }}:latest"
              fi
            fi
            echo "EOF"
            echo "labels<<EOF"
            echo "org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}"
            echo "org.opencontainers.image.revision=${{ github.sha }}"
            echo "org.opencontainers.image.version=${{ needs.build.outputs.image_tag }}"
            echo "org.opencontainers.image.licenses=MIT"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          config-inline: |
            [registry."192.168.1.14:5050"]
              http = true
      # THE FLEET'S PUSH. Deployments update from this registry, so it goes
      # first and on its own: a public registry being unreachable, rate
      # limited or misconfigured must not be able to stop the image an
      # already-running cluster is waiting for. `latest` moves on every main
      # push here, which is what makes that update path work.
      - name: Build and push to the fleet registry (multi-arch amd64 + arm64)
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.context }}/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ matrix.image }}:${{ needs.build.outputs.image_tag }}
            ${{ env.REGISTRY }}/${{ matrix.image }}:latest
          labels: ${{ steps.tags.outputs.labels }}
      # The public mirrors, best-effort. Cached layers make this nearly free,
      # and a failure here is a publishing problem rather than an outage.
      - name: Build and push to the public registries
        continue-on-error: true
        if: steps.tags.outputs.public != ''
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.context }}/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.tags.outputs.public }}
          labels: ${{ steps.tags.outputs.labels }}

  central:
    # Maven Central, release tags only. Immutable once accepted: the bundle is
    # uploaded and left in a validated state for a human to release from the
    # Central Portal, rather than auto-published.
    needs: build
    if: needs.build.outputs.is_release == 'true'
    # self-hosted for the same budget reason as build; release tags only
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v4
      - name: Refuse to build a bundle without a signing key
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
        run: |
          if [ -z "$SIGNING_KEY" ]; then
            echo "::error::SIGNING_KEY is not set — Central rejects unsigned artifacts."
            echo "An unsigned bundle would fail validation after upload, which is a" >&2
            echo "worse place to find out. Set SIGNING_KEY and SIGNING_PASSWORD." >&2
            exit 1
          fi
      - name: Stage and bundle
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
          SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
        run: ./gradlew centralBundle --no-daemon ${{ needs.build.outputs.gradle_args }}
      - name: Upload to the Central Portal
        env:
          CENTRAL_TOKEN: ${{ secrets.CENTRAL_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "$CENTRAL_TOKEN" ]; then
            echo "::error::CENTRAL_TOKEN is not set."
            exit 1
          fi
          code=$(curl --silent --show-error --fail-with-body \
            --output /tmp/central-response.txt --write-out '%{http_code}' \
            --request POST \
            --header "Authorization: Bearer $CENTRAL_TOKEN" \
            --form "bundle=@build/central/dbo-central-bundle.zip" \
            "https://central.sonatype.com/api/v1/publisher/upload?name=dbo-${GITHUB_REF_NAME}&publishingType=USER_MANAGED")
          echo "HTTP $code"
          cat /tmp/central-response.txt
          echo
          echo "Deployment staged. Validate and release it from" \
               "https://central.sonatype.com/publishing/deployments"
      - name: Keep the bundle as a build artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: central-bundle
          path: build/central/dbo-central-bundle.zip
          retention-days: 14
