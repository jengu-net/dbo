# THE test dials. They live here, not in build.yml, because a value that only
# exists in a workflow can only be run by retyping it — and three commits
# landed on a red main while "the full build is green locally" was true and
# meaningless.
#
# Measured inside a bounded container, which is the only place a floor can be
# measured honestly: a laptop with spare RAM fails these suites on timing long
# before it fails on heap. The CI that set these was a 2500m container, and
# the numbers were sized to leave room in it for the runner agent and the
# Gradle daemon, which share the space. The true floor is lower —
# dbo-fhir-element passes at 700m — but the harness needs somewhere above
# 1200m: below that HAPI dies as a null-message HAPI-2330, which is what an
# OOM looks like through HAPI. A test JVM whose CEILING alone exceeds its
# container dies loudly as exit 137 naming no test at all, and only when a
# suite actually reaches for it; that happened on 2026-08-28.
#
# CI now runs on GitHub-hosted runners with more memory than that container
# had. The dials stay where they were proven until a run there shows they
# should move; raising them because the room exists is how the local floor
# and the CI floor drift apart again.
#
# CI passes NO test flags: it runs ./verify, which reads exactly this file, so
# the workflow and a laptop cannot disagree about what the check is.
#
# Raising either number is opting IN, per invocation:
#   ./gradlew test -PdboTestHeap=4g -PdboTestParallelism=4
# and what actually applied is printed by every test task ("test jvm: ...").
#
# Clearing them is not a way to get "the default": each module still carries
# its own minimum (dbo-fhir-element 4g, the r4/r5 faces 2g) and that is what
# applies with no property set, because Gradle's own default heap dies inside
# HAPI as a null-message HAPI-2330 that names nothing.
dboTestHeap=2g
# One, and measured rather than hoped for. The suite is WRITTEN for
# concurrency — junit-platform.properties has the IT classes taking a
# database each and class-unique tenant codes — and running it that way is
# both slower and redder:
#
#   parallelism 1   phase 1 in 17m, green
#   parallelism 2   phase 1 in 48m, seven failing
#   parallelism 4   throughput no better; the JVM held 0.45g of its 2g
#   parallelism 8   throughput FELL; the JVM held 1.57g of its 2g
#
# Slower because the suite is not short of threads. Sixty-one of its classes
# build a tenant, each one materialising ninety megabytes of face into a
# fresh database on ONE shared Postgres — so the threads queue at the server
# and the waiting moves rather than shrinking. What makes this suite faster
# is cheaper fixtures, not more workers.
#
# Redder because three classes do not survive a second tenant coming up
# beside them: AZoneReachesAnotherFaceThroughOneProjectionIT (a projected
# zone never becomes servable), ProvisionedSecretIT (a dependent waiting on
# its upstream never returns) and TenantRuntimeIT (a tenant answers 404 for
# a type it serves). Those are the work, and the dial is here for when they
# are done.
dboTestParallelism=1
# The other pool, off by default: forks are JVMs, and each one pays for its
# own heap and its own Postgres container. Threads above are the cheap axis.
# dboTestForks=2
# The daemon shares the container with the test JVM it forks, and was
# unbounded — the one number in that 2500m nobody had said anything about.
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=384m
